import '../styles.css'

const ViewItem = ({ id, title, price, rating, image, category }) => {
    return (
        <div class='view--item'>
            
        </div>
    )
}

export default ViewItem
